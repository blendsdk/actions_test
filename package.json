{
  "name": "root",
  "private": true,
  "scripts": {
    "build":"lerna run build",
    "test":"lerna run test",
    "release":"yarn build && yarn test"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^3.22.1"
  }
}