{
  "name": "actions_test",
  "private":true,
  "repository": "git@github.com:blendsdk/actions_test.git",
  "author": "Gevik Babakhani <gevik@blendjs.com>",
  "license": "MIT",
  "scripts": {
    "publish-packages":"lerna publish from-package",
    "tag-version":"lerna version --conventional-commits --no-git-tag-version --yes",
    "release":"yarn build && yarn test && yarn tag-version && git add . && git commit -am\"Creating release `date`\"",
    "build":"lerna run build",
    "test":"lerna run test"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^3.22.1"
  }
}